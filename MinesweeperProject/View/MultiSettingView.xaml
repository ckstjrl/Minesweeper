<UserControl x:Class="MinesweeperProject.View.MultiSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="LIGButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#002C77"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="buttonScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EBF2FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#002C77"/>
                                <Setter Property="Foreground" Value="#002C77"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(RenderTransform).ScaleX" To="0.97" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(RenderTransform).ScaleY" To="0.97" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(RenderTransform).ScaleX" To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(RenderTransform).ScaleY" To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LIGExitButtonStyle" TargetType="Button" BasedOn="{StaticResource LIGButtonStyle}">
            <Setter Property="Background" Value="#FEEBEE"/>
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Setter Property="BorderBrush" Value="#FFCDD2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFCDD2"/>
                    <Setter Property="BorderBrush" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LIGRoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Margin="30,25,0,20">
            <TextBlock Text="LIGNex1" Foreground="#002C77" FontWeight="Bold" FontSize="13" Margin="0,0,0,2"/>
            <TextBlock Text="멀티플레이 설정" Foreground="#1A1A1A" FontWeight="ExtraBold" FontSize="34"/>
        </StackPanel>

        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">

                <Border Background="#F8F9FA" CornerRadius="15" Padding="12" Margin="0,0,0,30" HorizontalAlignment="Left" BorderBrush="#EEEEEE" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👤" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="서버 IP 주소" FontSize="14" FontWeight="Bold" Foreground="#888888" Margin="8,0,0,8"/>
                    <TextBox Style="{StaticResource LIGRoundedTextBoxStyle}"
                             Height="45"
                             Text="{Binding ServerIp, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="16"/>
                </StackPanel>

                <Button Content="방 만들기" Command="{Binding MakeRoomCommand}" Style="{StaticResource LIGButtonStyle}" Height="50" Margin="0,5" />
                <Button Content="방 참가하기" Command="{Binding IncomeRoomCommand}" Style="{StaticResource LIGButtonStyle}" Height="50" Margin="0,5"/>
                <Button Content="돌아가기" Command="{Binding CloseCommand}" Style="{StaticResource LIGExitButtonStyle}" Height="50" Margin="0,20"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>